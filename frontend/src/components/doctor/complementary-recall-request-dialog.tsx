import { useState } from "react";
import { tripoliDateTimeLocalToIso } from "@/lib/date-format";
import type { ComplementaryRecallRequestPayload } from "@/lib/api/doctor-portal-reporting";
import type { ComplementaryRecallQaClassification, ComplementaryRecallReasonCode, ComplementaryRecallReportingDisposition, ComplementaryRecallUrgency } from "@/lib/api/complementary-recalls";
import { Button, Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, Input, Textarea } from "@/components/shared";

export function ComplementaryRecallRequestDialog({ open, onClose, examLabel, submitting, error, onSubmit }: {
  open: boolean;
  onClose: () => void;
  examLabel: string;
  submitting: boolean;
  error?: string | null;
  onSubmit: (payload: ComplementaryRecallRequestPayload) => void;
}) {
  const [reasonCode, setReasonCode] = useState<ComplementaryRecallReasonCode | "">("");
  const [qaClassification, setQaClassification] = useState<ComplementaryRecallQaClassification | "">("");
  const [urgency, setUrgency] = useState<ComplementaryRecallUrgency>("routine");
  const [dueAt, setDueAt] = useState("");
  const [reportingDisposition, setReportingDisposition] = useState<ComplementaryRecallReportingDisposition>("supplement_original_report");
  const [receptionInstruction, setReceptionInstruction] = useState("");
  const [technologistInstruction, setTechnologistInstruction] = useState("");
  const submit = () => {
    if (!reasonCode || !qaClassification || !technologistInstruction.trim()) return;
    onSubmit({ reasonCode, qaClassification, urgency, dueAt: tripoliDateTimeLocalToIso(dueAt), reportingDisposition, receptionInstruction: receptionInstruction.trim() || null, technologistInstruction: technologistInstruction.trim() });
  };
  return <Dialog open={open} onClose={() => !submitting && onClose()}><DialogContent maxWidth="560px" className="max-h-[calc(100dvh-1rem)] overflow-hidden"><DialogHeader><DialogTitle>Request additional imaging</DialogTitle><DialogDescription>Original exam: {examLabel}</DialogDescription></DialogHeader><div className="grid max-h-[60dvh] gap-3 overflow-y-auto pe-1"><div className="grid gap-3 sm:grid-cols-2"><label className="grid gap-1 text-sm font-medium">Reason<select required aria-label="Recall reason" value={reasonCode} onChange={(event) => setReasonCode(event.target.value as ComplementaryRecallReasonCode)} className="input-premium font-normal"><option value="">Select reason</option><option value="missing_sequence_phase">Missing sequence or phase</option><option value="incomplete_anatomical_coverage">Incomplete anatomical coverage</option><option value="motion_nondiagnostic_quality">Motion or nondiagnostic quality</option><option value="incorrect_protocol">Incorrect protocol</option><option value="incorrect_contrast_phase_timing">Incorrect contrast phase/timing</option><option value="additional_diagnostic_characterization">Additional diagnostic characterization</option><option value="technical_equipment_problem">Technical equipment problem</option><option value="patient_related_limitation">Patient-related limitation</option><option value="other">Other</option></select></label><label className="grid gap-1 text-sm font-medium">QA classification<select required aria-label="QA classification" value={qaClassification} onChange={(event) => setQaClassification(event.target.value as ComplementaryRecallQaClassification)} className="input-premium font-normal"><option value="">Select classification</option><option value="diagnostic_addition">Diagnostic addition</option><option value="technical_repeat">Technical repeat</option><option value="protocol_error">Protocol error</option><option value="acquisition_error">Acquisition error</option><option value="equipment_failure">Equipment failure</option><option value="patient_related_unavoidable">Patient-related unavoidable</option><option value="other">Other</option></select></label><label className="grid gap-1 text-sm font-medium">Urgency<select aria-label="Urgency" value={urgency} onChange={(event) => setUrgency(event.target.value as ComplementaryRecallUrgency)} className="input-premium font-normal"><option value="same_day">Same day</option><option value="within_24_hours">Within 24 hours</option><option value="within_72_hours">Within 72 hours</option><option value="routine">Routine</option></select></label><label className="grid gap-1 text-sm font-medium">Due date/time<Input type="datetime-local" value={dueAt} onChange={(event) => setDueAt(event.target.value)} /></label><label className="grid gap-1 text-sm font-medium sm:col-span-2">Reporting disposition<select aria-label="Reporting disposition" value={reportingDisposition} onChange={(event) => setReportingDisposition(event.target.value as ComplementaryRecallReportingDisposition)} className="input-premium font-normal"><option value="supplement_original_report">Supplement original report</option><option value="separate_report">Separate report</option><option value="no_separate_report">No separate report</option></select></label></div><label className="grid gap-1 text-sm font-medium">Reception instruction<Textarea value={receptionInstruction} onChange={(event) => setReceptionInstruction(event.target.value)} className="min-h-20 font-normal" /></label><label className="grid gap-1 text-sm font-medium">Technologist instruction<Textarea required value={technologistInstruction} onChange={(event) => setTechnologistInstruction(event.target.value)} className="min-h-24 font-normal" /></label>{error ? <p role="alert" className="text-sm text-red-700">{error}</p> : null}</div><DialogFooter><Button variant="secondary" onClick={onClose} disabled={submitting}>Cancel</Button><Button onClick={submit} disabled={!technologistInstruction.trim() || !reasonCode || !qaClassification || submitting}>{submitting ? "Requesting..." : "Request additional imaging"}</Button></DialogFooter></DialogContent></Dialog>;
}
