import { useEffect, useState } from "react";

type Settings = { enabled: boolean; server: string; share: string; domain: string; username: string; passwordConfigured: boolean; incomingSubfolder: string; processedSubfolder: string; failedSubfolder: string; pollingIntervalSeconds: number; fileReadyDelaySeconds: number; password?: string };
async function api<T>(path: string, options?: RequestInit): Promise<T> { const response = await fetch(`/api/settings/request-scan-automation${path}`, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options }); if (!response.ok) { const data = await response.json().catch(() => ({})); throw new Error(data.message || "Request Scan settings action failed"); } return response.json() as Promise<T>; }
export default function RequestScanAutomationSection() { const [settings, setSettings] = useState<Settings | null>(null); const [password, setPassword] = useState(""); const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false); useEffect(() => { void api<{ settings: Settings }>("").then((data) => setSettings(data.settings)).catch((error: Error) => setMessage(error.message)); }, []); if (!settings) return <p>{message ?? "Loading Request Scan Automation settings…"}</p>; const change = (key: keyof Settings, value: string | boolean | number) => setSettings({ ...settings, [key]: value }); const save = async () => { setBusy(true); setMessage(null); try { const result = await api<{ settings: Settings }>("", { method: "PUT", body: JSON.stringify({ ...settings, password }) }); setSettings(result.settings); setPassword(""); setMessage("Settings saved."); } catch (error) { setMessage(error instanceof Error ? error.message : "Save failed."); } finally { setBusy(false); } }; const test = async () => { setBusy(true); setMessage(null); try { await api("/test", { method: "POST" }); setMessage("SMB connection succeeded."); } catch (error) { setMessage(error instanceof Error ? error.message : "Connection test failed."); } finally { setBusy(false); } }; return <div className="space-y-4"><p className="text-sm text-muted-foreground">Monitor one SMB Incoming folder for PDF and JPEG appointment requests.</p><div className="grid gap-3 md:grid-cols-2"><label>Enabled<select className="input-premium mt-1 w-full" value={settings.enabled ? "enabled" : "disabled"} onChange={(event) => change("enabled", event.target.value === "enabled")}><option value="disabled">Disabled</option><option value="enabled">Enabled</option></select></label><label>SMB server<input className="input-premium mt-1 w-full" value={settings.server} onChange={(event) => change("server", event.target.value)} /></label><label>SMB share<input className="input-premium mt-1 w-full" value={settings.share} onChange={(event) => change("share", event.target.value)} /></label><label>Domain/workgroup<input className="input-premium mt-1 w-full" value={settings.domain} onChange={(event) => change("domain", event.target.value)} /></label><label>Username<input className="input-premium mt-1 w-full" value={settings.username} onChange={(event) => change("username", event.target.value)} /></label><label>Password<input type="password" className="input-premium mt-1 w-full" placeholder={settings.passwordConfigured ? "Saved password (leave blank to keep)" : "Password"} value={password} onChange={(event) => setPassword(event.target.value)} /></label><label>Incoming subfolder<input className="input-premium mt-1 w-full" value={settings.incomingSubfolder} onChange={(event) => change("incomingSubfolder", event.target.value)} /></label><label>Processed subfolder<input className="input-premium mt-1 w-full" value={settings.processedSubfolder} onChange={(event) => change("processedSubfolder", event.target.value)} /></label><label>Failed subfolder<input className="input-premium mt-1 w-full" value={settings.failedSubfolder} onChange={(event) => change("failedSubfolder", event.target.value)} /></label><label>Polling interval (seconds)<input type="number" min="1" className="input-premium mt-1 w-full" value={settings.pollingIntervalSeconds} onChange={(event) => change("pollingIntervalSeconds", Number(event.target.value))} /></label><label>File-ready delay (seconds)<input type="number" min="1" className="input-premium mt-1 w-full" value={settings.fileReadyDelaySeconds} onChange={(event) => change("fileReadyDelaySeconds", Number(event.target.value))} /></label></div>{message && <p className="text-sm">{message}</p>}<div className="flex gap-2"><button className="btn-primary" disabled={busy} onClick={() => void save()}>Save</button><button className="btn-secondary" disabled={busy} onClick={() => void test()}>Test connection</button></div></div>; }
