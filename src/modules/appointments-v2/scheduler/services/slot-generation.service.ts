/**
 * Appointments V2 — Slot generation service (stub).
 *
 * TODO (Stage 5): Generate time slots for a given date/modality.
 */

import type { Slot } from "../models/slot.js";

export function generateSlotsForDate(
  _modalityId: number,
  _date: string
): Slot[] {
  // TODO: Generate slots based on modality schedule
  return [];
}
