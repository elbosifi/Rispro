/**
 * Appointments V2 — Appointment mapper.
 *
 * TODO (Stage 6): Map DB rows to API response DTOs.
 */

import type { AppointmentResponseDto } from "../dto/appointment.dto.js";

export function mapToAppointmentResponse(
  _row: unknown
): AppointmentResponseDto {
  // TODO: Implement mapping (Stage 6)
  return {} as AppointmentResponseDto;
}
