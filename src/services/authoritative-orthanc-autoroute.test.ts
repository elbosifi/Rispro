import assert from "node:assert/strict";import test from "node:test";import {readFile} from "node:fs/promises";
test("auto-routing keeps ordinary series and fail-closed suppresses reconciliation modifications and audit SC",async()=>{const lua=await readFile(new URL("../../docs/examples/authoritative-orthanc/rispro-autoroute.lua",import.meta.url),"utf8");assert.match(lua,/OnStableSeries/);assert.match(lua,/ModifiedFrom/);assert.match(lua,/rispro_patient_identity_reconciliation_source/);assert.match(lua,/rispro_patient_identity_reconciliation/);assert.match(lua,/if not ok then return nil/);assert.match(lua,/if MustSuppress\(seriesId, metadata\) then return end/);assert.match(lua,/rispro_route_/);});
